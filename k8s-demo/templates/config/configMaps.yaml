kind: ConfigMap 
apiVersion: v1 
metadata:
  name: demo-config 
  namespace: nstest
data:
  # Configuration values can be set as key-value properties
  database: mongodb
  database_uri: mongodb://localhost:27017
  LOG_LEVEL: "1"
